📝 2026.04.14 [Study Note] 

# ✅ 1. 오늘 해결한 문제 (Total 4)
- [신규] 백준 1316: 그룹 단어 체커 (과거 기록장과 이전 문자 비교를 통한 필터링) <br>
- [신규] 백준 1436: 영화감독 숌 (while True 무한 루프와 문자열 포함 여부 검사) <br>
- [신규] 백준 4673: 셀프넘버 (분해합 로직과 set을 이용한 소거법) <br>
- [신규] 백준 10448: 유레카 이론 (3중 for문을 이용한 조합 탐색 및 함수 조기 종료) <br>



# 🧠 2. 핵심 로직 복기: '과거 기록장' 과 '3중 조합 탐색'
단순히 현재만 보는 것이 아닌, '과거에 이런 일이 있었나?' 를 확인하는 로직과 '모든 조합을 뒤져보는' 부분을 복습하였음.

```Python
# 1316: 그룹 단어 판별의 핵심 (이전 문자와 기록장 비교)
seen = []
prev_char = ""

for char in word:
    if char != prev_char:               # 새로운 문자가 등장했는데
        if char in seen:                # 이미 과거에 나왔던 적이 있다면?
            return False                # 그룹단어 탈락
        seen.append(char)               # 처음 본 거면 기록장에 추가
    prev_char = char                    # 다음 바퀴를 위해서 현재 문자를 '이전 문자' 로 지정

# 10448: 3중 for문과 함수 탈출
def check():
    for i in triangle_nums:
        for j in triangle_nums:
            for k in triangle_nums:
                if i + j + k == target:
                    return 1            # 찾으면 즉시 전체 루핑 탈출

    return 0                            # 끝까지 못찾은거면 0 임
```



# 🚨 3. 피드백 및 부족했던 점
- 연속성과 재출현의 구분 : 단순히 "이미 나온적이 있었나" 만 체크하면 aa 처럼 연속된 경우도 탈락시킬 수 있는 리스크가 있었음.
    - 반드시 직전 문자와 다른가? 를 비교해야함. 이런 케이스는 일단 "" 처럼 공백을 주는 변수를 하나 생성하고 거기에 챔피언 변수를 비교하는 방법을
    써서 과거와 비교할 필요가 있었음.

- 다중 반복문 탈출의 어려움 : 3중 for 문 안에서 break 를 쓰면 안쪽 루프 하나만 빠져나와버리는 케이스가 존재했음.
    - 이런 경우는 함수 def() 로 구현해서 return 으로 한방에 죽이는 방식을 사용.

  

# 🚀 4. 내일의 스킬업 전략
- for-else 문 적극 활용 : for 문이 break 없이 끝까지 완주했을 때만 실행되는 else 블록을 활용해서 코드 가독성을 높일 수 있고

- 데이터 미리 만들기 : 반복문 안에서 매번 계산하는 것 보다, 직접 비교 할 데이터를 리스트에 미리 뽑아놓고 시작하면 편하게 접근 할 수 있는 케이스가 많음.

- 문자열 포함 확인 (in) : if "666" in str(num): 처럼 숫자를 문자열로 바꿔서 부분 일치를 확인하는 기법을 자유자재로 사용해보기.



# ✍ 5. 내일 다시 풀어보면 좋을 문제 : 
- [10448] 유레카 이론