📝 2026.04.09 [Study Note] 

# ✅ 1. 오늘 해결한 문제 (Total 4) <br>
- [2562] 최댓값 (1D): 리스트 없이 입력 즉시 최댓값과 인덱스를 갱신하는 효율적 탐색. <br>
- [10818] 최소, 최대: 정렬(O(N log N))보다 효율적인 순차 탐색(O(N))의 가치 이해. <br>
- [3040] 백설 공주와 일곱 난쟁이: '가짜 2명 색출' 로직을 2중 for문 조합으로 구현. <br>
- [2566] 최댓값 (2D): 2차원 공간에서의 챔피언 갱신 로직 복습 완료. <br>


# 🧠 2. 핵심 로직 복기: '가짜 2명 찾기' (조합 탐색)
- [3040] 백설 공주와 일곱 난쟁이 문제에서 9명 중 7명을 선택해야 하는 문제에서 7명을 찾기 위해 for 문을 7개 돌리는게 아니라, 9개 중 가짜 2명을 찾아서 2개 for 문으로 가짜를 찾아 빼버리는 로직으로 문제를 해결 할 수 있었음. <br> <br>

```Python
# 핵심 포인트: j의 시작 범위는 보통 i+1 로 시작하는게 정석, 왜냐하면 2중 for 문의 경우 대부분의 케이스가 첫 번째 for 문과 겹치지 않는 걸 계산하기 위함으로 선언되는 케이스가 많은데 첫 번째 for 문의 i 와 똑같이 시작해버리면 겹쳐버리는 케이스가 존재하기 때문에
for i in range(9):
    for j in range(i+1, 9):
        if total_sum - (miniboy_list[i] + miniboy_list[j]) == 100:
            # 가짜 범인이 잡히는 로직
```


# 🚨 3. 피드백 및 부족했던 점
- 정렬(Sort) 함정 : 최댓값/최솟값 만 찾으면 되는 문제에서 무조건 sort() 를 쓴 다음에 그 List 의 맨 처음과 맨 뒤의 값을 뽑아서 최대, 최소를 찾게되면 나중에 데이터가 방대해졌을 때 sort() 로 인해서 실행시간이 길어지는 케이스가 발생 할 수 있음. (오늘의 [10818] 최소, 최대 문제) <br>
    - 따라서 for 문으로 1회 순회하면서 챔피언 변수를 따로 설정해서 챔피언 변수랑 for 문을 돌면서 나오는 인덱스랑 하나씩 비교하면서 조건에 부합하면 변수에 넣는 형식으로 진행하는 것이 실행시간에 더 효율적일 수 있음. <br> <br>


- 중복 탐색의 오류 : 2중 for 문에서 j 범위를 range(9) 하나만 선언하면 자기 자신을 고르는 오류가 발생
  - 조합을 찾을 때 안쪽 반복문의 시작점을 i + 1 로 두는 습관을 들여야 함.

    
# 🚀 4. 내일의 스킬업 전략
- 초기값 설정의 정석 : 최솟값을 찾을 때 miv_val = 0 으로 두면 갱신되지 않는 오류가 생길 수 있음.
  따라서 리스트의 첫 번째 값 list[0] 으로 초기화 하는 것이 일단 가장 안전 함.

- 메모리 효율 : 2562번 처럼 데이터를 다 저장할 필요가 없다면 리스트를 쓰지 않고 변수 하나로 해결하는 부분으로 접근할 필요가 있음

- 문제 해석력 : "줄을 세워야 되나?" 또는 "하나만 찾으면 돼?" 라는 문제설정에 대해서 먼저 판단하고 알고리즘을 선택 할 필요가 있겠음.


# ✍ 5. 내일 다시 풀어보면 좋을 문제 
- [3040] 백설 공주와 일곱 난쟁이
