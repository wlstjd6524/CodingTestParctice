✍ 2026.03.23 학습 회고

1. set(), in
    - A가 B 바구니 안에 있나? 라는 형태의 문제는 List 로 담는 것 보다 Set() 이 훨씬 유리함
    (내부적으로 해시 테이블이 작동해서 O(1) 만에 존재 여부를 찾을 수 있기 때문에) => 리스트는 전체적으로 순회 해야함.

    ex)
    cards_set = set([1, 2, 3, 4, 5])
    print(3 in cards_set)

2. 가로로 이어 붙이기 end = " "
    - print("1", end = " ")

3. Set() 에서는 합,교,차집합에 대한 부분을 구현할 수 있다.
    - ex)
    # A: 듣도 못한 사람, B: 보도 못한 사람
    A = {'apple', 'banana', 'cherry'}
    B = {'banana', 'kiwi'}

    print(A & B)        # 교집합
    print(A | B)        # 합집합
    print(A - B)        # 차집합

4. Set() 에서는 탐색속도가 빠른 대신 순서를 무시해버리기 때문에 sorted() 를 통해 정렬해서 찾으면 훨씬 더 유리하게 사용이 가능하다
    - ex)
    messy_set = {'A', 'B', 'C'}
    
    clean_lsit = sorted(messy_set)
    print(clean_lsit)


<-- 동빈나의 코테 강의 -->
- 금일 과정 : 3강 - 수 자료형

1. 리스트 컴프리헨션
    ex) array = [i for i in range(20) if i % 2 == 1]
    print(array)

    - 식으로 리스트 선언과 동시에 조건문과 반복문을 한꺼번에 선언함으로서 코드를 경량화가 가능하다.

    - 리스트 컴프리헨션은 2차원 리스트를 초기화 할 때 효과적으로 사용할 수 있다.
    특히 N * M 크기의 2차원 리스트를 한 번에 초기화 해야 할 때 매우 유용하다. ex) array = [[0] * m for _ in range(n)]
    - 만약 2차원 리스트를 초기화 할 때 다음과 같이 작성하면 예기치 않은 결과가 나올 수 있다.
        - ex) array = [[0] * m] * N
        - 위 예시는 "전체 리스트 안에 포함된 각 리스트가 모두 같은 객체로 인식되게 된다."


2. 리스트 관련 코테에서 많이 쓰일법한 메서드
    - append() 
        - 사용법 : 변수명.append()
        - 리스트에 원소를 하나 삽입할 때 사용한다.
        - 시간 복잡도 : O(1)
    
    - sort()
        - 사용법 : 변수명.sort() => 오름차순으로 정렬 | 변수명.sort(reverse=True) => 내림차순으로 정렬
        - 시간 복잡도 : O(NLogN)
    
    - reverse()
        - 사용법 : 변수명.reverse()
        - 리스트의 원소의 순서를 모두 뒤집어 놓는다.
        - 시간 복잡도 : O(N)

    - insert()
        - 사용법 : insert(삽입할 위치 인덱스, 삽입 할 값)
        - 특정한 인덱스 위치에 원소를 삽입할 때 사용한다.
        - 시간 복잡도 : O(N)
    
    - count()
        - 사용법 : 변수명.count(특정 값)
        - 리스트에서 특정한 값을 가지는 데이터의 개수를 셀 때 사용한다.
        - 시간 복잡도 : O(N)

    - remove()
        - 사용법 : 변수명.remove()
        - 특정한 값을 갖는 원소를 제거하는데, 값을 가진 원소가 여러 개면 하나만 제거한다.
        - 시간 복잡도 : O(N)


3. 튜플 자료형 
    - 튜플 자료형은 리스트와 유사하지만 '한 번 선언된 값을 변경할 수 없고' , '리스트는 [] 대괄호를 이용하지만, 튜플은 () 소괄호를 이용'
    - 튜플은 리스트에 비해 상대적으로 공간 효율적이다. (메모리를 더 적게 차지함)

    - 튜플을 사용하면 좋은 케이스는
        - 서로 다른 성질의 데이터를 묶어서 관리할 때
        => 최단 경로 알고리즘에서는 (비용, 노드번호)의 형태로 튜플 자료형을 자주 사용함.

        - 데이터의 나열을 해싱(Hashing)의 키 값으로 사용해야 할 때
        => 튜플은 변경이 불가능해서 리스트와 다르게 키 값으로 사용 될 수 있음.

        - 리스트보다 메모리를 효율적으로 사용해야 할 때


4. 사전 자료형 (딕셔너리)
    - 사전 자료형은 Key 와 Value 의 쌍을 데이터로 가지는 자료형.
    => 리스트나 튜플이 값을 순차적으로 저장하는 것과는 대비됨.

    - 사전 자료형은 키와 값의 쌍을 데이터로 가지며, 원하는 변경 불가능한 자료형을 키로 사용 할 수 있다.
    - 파이썬 사전 자료형은 해시 테이블을 이용해서 데이터의 조회 및 수정에 있어서 O(1) 시간에 처리할 수 있음.

    - 사전 자료형에서는 키와 값을 별도로 뽑아내기 위한 메서드를 지원.
        => 키 데이터만 뽑아서 리스트로 이용할 때는 keys()
        => 값 데이터만 뽑아서 리스트로 이용할 떄는 values()


5. 집합 자료형
    - 집합은 다음과 같은 특징이 있음
        - 중복을 허용하지 않고
        - 순서가 없음
    
    - 집합은 리스트 혹은 문자열을 이용해서 초기화 할 수 있음
        => 이 때 set() 함수 이용!

    - 혹은 중괄호 안에 각 원소를 콤마를 기준으로 구분하여 삽입함으로써 초기화도 가능
    - 데이터의 조회 및 수정에 있어서 O(1) 의 시간에 처리 가능.